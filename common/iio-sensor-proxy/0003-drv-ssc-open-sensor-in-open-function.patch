diff -ruN iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-accel.c iio-patched/src/drv-ssc-accel.c
--- iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-accel.c	2026-03-02 21:19:51.000000000 +0200
+++ iio-patched/src/drv-ssc-accel.c	2026-07-02 02:23:06.315662107 +0300
@@ -83,18 +83,33 @@
 
 	drv_data = (DrvData *) sensor_device->priv;
 	drv_data->sensor = ssc_sensor_accelerometer_new_sync (NULL, &error);
-	if (!drv_data->sensor)
+	if (!drv_data->sensor) {
 		g_warning ("Creating SSC accelerometer sensor failed: %s", error->message);
-	else
-		g_object_get (drv_data->sensor,
-		              SSC_SENSOR_NAME, &sensor_device->name,
-		              NULL);
+		g_free (drv_data);
+		g_free (sensor_device);
+		return NULL;
+	}
+
+	g_object_get (drv_data->sensor,
+	              SSC_SENSOR_NAME, &sensor_device->name,
+	              NULL);
 
 	/* Setup accel attributes */
 	drv_data->mount_matrix = setup_mount_matrix (device);
 	drv_data->location = setup_accel_location (device);
 	set_accel_scale (&drv_data->scale, 1.0);
 
+	/* Open the sensor now so it starts streaming data. The
+	 * measurement signal is connected in set_polling() when a
+	 * D-Bus client claims the sensor. */
+	if (!ssc_sensor_accelerometer_open_sync (drv_data->sensor, NULL, &error)) {
+		g_warning ("Enabling SSC accelerometer sensor failed: %s", error->message);
+		g_clear_object (&drv_data->sensor);
+		g_free (drv_data);
+		g_free (sensor_device);
+		return NULL;
+	}
+
 	return sensor_device;
 }
 
@@ -136,6 +151,11 @@
 	g_autoptr(GError) error = NULL;
 	DrvData *drv_data = (DrvData *) sensor_device->priv;
 
+	if (drv_data->sensor) {
+		g_clear_signal_handler (&drv_data->measurement_id, drv_data->sensor);
+		ssc_sensor_accelerometer_close_sync (drv_data->sensor, NULL, &error);
+	}
+
 	g_clear_object (&drv_data->sensor);
 	g_clear_pointer (&drv_data->mount_matrix, g_free);
 	g_clear_pointer (&sensor_device->priv, g_free);
diff -ruN iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-compass.c iio-patched/src/drv-ssc-compass.c
--- iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-compass.c	2026-03-02 21:19:51.000000000 +0200
+++ iio-patched/src/drv-ssc-compass.c	2026-07-02 02:23:06.315734393 +0300
@@ -112,8 +112,15 @@
 static void
 ssc_compass_close (SensorDevice *sensor_device)
 {
+	g_autoptr(GError) error = NULL;
 	DrvData *drv_data = (DrvData *) sensor_device->priv;
 
+	if (drv_data->sensor) {
+		g_clear_signal_handler (&drv_data->measurement_id, drv_data->sensor);
+		if (!ssc_sensor_compass_close_sync (drv_data->sensor, NULL, &error))
+			g_warning ("Closing SSC compass sensor failed: %s", error->message);
+	}
+
 	g_clear_object (&drv_data->sensor);
 	g_clear_pointer (&sensor_device->name, g_free);
 	g_clear_pointer (&sensor_device->priv, g_free);
diff -ruN iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-light.c iio-patched/src/drv-ssc-light.c
--- iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-light.c	2026-03-02 21:19:51.000000000 +0200
+++ iio-patched/src/drv-ssc-light.c	2026-07-02 02:23:06.315775201 +0300
@@ -112,6 +112,12 @@
 	g_autoptr(GError) error = NULL;
 	DrvData *drv_data = (DrvData *) sensor_device->priv;
 
+	if (drv_data->sensor) {
+		g_clear_signal_handler (&drv_data->measurement_id, drv_data->sensor);
+		if (!ssc_sensor_light_close_sync (drv_data->sensor, NULL, &error))
+			g_warning ("Closing SSC light sensor failed: %s", error->message);
+	}
+
 	g_clear_object (&drv_data->sensor);
 	g_clear_pointer (&sensor_device->priv, g_free);
 	g_free (sensor_device);
diff -ruN iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-proximity.c iio-patched/src/drv-ssc-proximity.c
--- iio-fix/iio-sensor-proxy-0085ddf8ecb1/src/drv-ssc-proximity.c	2026-03-02 21:19:51.000000000 +0200
+++ iio-patched/src/drv-ssc-proximity.c	2026-07-02 02:23:06.315878206 +0300
@@ -112,8 +112,15 @@
 static void
 ssc_proximity_close (SensorDevice *sensor_device)
 {
+	g_autoptr(GError) error = NULL;
 	DrvData *drv_data = (DrvData *) sensor_device->priv;
 
+	if (drv_data->sensor) {
+		g_clear_signal_handler (&drv_data->measurement_id, drv_data->sensor);
+		if (!ssc_sensor_proximity_close_sync (drv_data->sensor, NULL, &error))
+			g_warning ("Closing SSC proximity sensor failed: %s", error->message);
+	}
+
 	g_clear_object (&drv_data->sensor);
 	g_clear_pointer (&sensor_device->priv, g_free);
 	g_free (sensor_device);
