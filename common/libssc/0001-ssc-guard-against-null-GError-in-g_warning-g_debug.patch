diff --git a/src/libssc-client.c b/src/libssc-client.c
index 1448312..f6e06ed 100644
--- a/src/libssc-client.c
+++ b/src/libssc-client.c
@@ -106,7 +106,7 @@ report_large_received (QmiClientSsc *self, QmiIndicationSscReportLargeOutput *ou
 	GArray *protobuf = NULL;
 
 	if (!qmi_indication_ssc_report_large_output_get_data (output, &protobuf, &error)) {
-		g_warning ("Cannot extract Protobuf data (large report): %s", error->message);
+		g_warning ("Cannot extract Protobuf data (large report): %s", error ? error->message : "unknown error");
 		return;
 	}
 
@@ -121,7 +121,7 @@ report_small_received (QmiClientSsc *self, QmiIndicationSscReportSmallOutput *ou
 	GArray *protobuf = NULL;
 
 	if (!qmi_indication_ssc_report_small_output_get_data (output, &protobuf, &error)) {
-		g_warning ("Cannot extract Protobuf data (small report): %s", error->message);
+		g_warning ("Cannot extract Protobuf data (small report): %s", error ? error->message : "unknown error");
 		return;
 	}
 
@@ -143,7 +143,7 @@ request_ready (QmiClientSsc *self, GAsyncResult *res, gpointer user_data)
 	}
 
 	if (!qmi_message_ssc_control_output_get_result (output, &error)) {
-		g_warning ("QMI request failed: %s", error->message);
+		g_warning ("QMI request failed: %s", error ? error->message : "unknown error");
 		qmi_message_ssc_control_output_unref (output);
 		g_task_return_error (task, error);
 		g_clear_object (&task);
@@ -214,7 +214,7 @@ ssc_client_send (SSCClient *self, guint64 uid_high, guint64 uid_low, guint32 mes
 	input = qmi_message_ssc_control_input_new ();
 
 	if (!qmi_message_ssc_control_input_set_report_type (input, QMI_SSC_REPORT_TYPE_LARGE, &error)) {
-		g_debug ("Inserting report type failed: %s", error->message);
+		g_debug ("Inserting report type failed: %s", error ? error->message : "unknown error");
 		qmi_message_ssc_control_input_unref (input);
 		g_task_return_error (task, error);
 		g_clear_object (&task);
@@ -222,7 +222,7 @@ ssc_client_send (SSCClient *self, guint64 uid_high, guint64 uid_low, guint32 mes
 	}
 
 	if (!qmi_message_ssc_control_input_set_data (input, buf, &error)) {
-		g_debug ("Inserting protobuf data failed: %s", error->message);
+		g_debug ("Inserting protobuf data failed: %s", error ? error->message : "unknown error");
 		qmi_message_ssc_control_input_unref (input);
 		g_task_return_error (task, error);
 		g_clear_object (&task);
@@ -429,7 +429,7 @@ ssc_client_dispose (GObject *object)
 
 	/* QMI client released, check result */
 	if (!qmi_device_release_client_finish (priv->device, ctx.result, &error))
-		g_warning ("Could not release SSC QMI client: %s\n", error->message);
+		g_warning ("Could not release SSC QMI client: %s\n", error ? error->message : "unknown error");
 
 	ssc_common_clear_sync_context (&ctx);
 
@@ -444,7 +444,7 @@ ssc_client_dispose (GObject *object)
 	ssc_common_wait_sync_context (&ctx);
 
 	if (!qmi_device_close_finish (priv->device, ctx.result, &error))
-		g_warning ("Could not close SSC QMI client: %s\n", error->message);
+		g_warning ("Could not close SSC QMI client: %s\n", error ? error->message : "unknown error");
 
 	g_clear_object (&priv->qmi_client_ssc);
 	g_clear_object (&priv->device);
diff --git a/src/libssc-sensor.c b/src/libssc-sensor.c
index 2cf686f..a2ced89 100644
--- a/src/libssc-sensor.c
+++ b/src/libssc-sensor.c
@@ -108,7 +108,7 @@ sensor_close_ready (SSCClient *self, GAsyncResult *result, gpointer user_data)
 	if (!ssc_client_send_finish (self, result, &error)) {
 		g_task_return_error (task, error);
 		g_clear_object (&task);
-		g_debug ("Sensor disable request failed: %s", error->message);
+		g_debug ("Sensor disable request failed: %s", error ? error->message : "unknown error");
 		return;
 	}
 
@@ -170,7 +170,7 @@ sensor_open_ready (SSCClient *self, GAsyncResult *result, gpointer user_data)
 	if (!ssc_client_send_finish (self, result, &error)) {
 		g_task_return_error (task, error);
 		g_clear_object (&task);
-		g_debug ("Sensor enable request failed: %s", error->message);
+		g_debug ("Sensor enable request failed: %s", error ? error->message : "unknown error");
 		return;
 	}
 
@@ -504,7 +504,7 @@ attribute_ready (SSCClient *self, GAsyncResult *result, gpointer user_data)
 	g_autoptr (GError) error = NULL;
 
 	if (!ssc_client_send_finish (self, result, &error)) {
-		g_warning ("Sensor attribute request failed: %s", error->message);
+		g_warning ("Sensor attribute request failed: %s", error ? error->message : "unknown error");
 		return;
 	}
 
