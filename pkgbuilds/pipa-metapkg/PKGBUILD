pkgname=pipa-metapkg
pkgver=1.1
pkgrel=14
pkgdesc="Xiaomi Pad 6 support meta package for EndeavourOS"
arch=('any')
license=('MIT')
depends=('linux-pipa' 'xiaomi-pipa-firmware' 'limine' 'pipa-dracut' 'pipa-limine-config' 'pipa-sound-conf' 'swclock-offset' 'bootmac' 'bluez-git' 'mesa' 'pipa-sensors' 'qrtr' 'rmtfs' 'tqftpserv' 'pd-mapper' 'wireless-regdb' 'tuned' 'tuned-ppd')

source=('90-pipa-gsk-renderer.sh')
sha256sums=('SKIP')

package() {
  mkdir -p "$pkgdir"
  install -Dm755 90-pipa-gsk-renderer.sh "$pkgdir/etc/profile.d/90-pipa-gsk-renderer.sh"
}
