From: Ayman Ragab <aymanrar2c@gmail.com>
Date: Tue, 30 Jun 2026 22:36:36 +0300
Subject: [PATCH] media: qcom: camss: Fix video pipeline stop streaming teardown

Continue best-effort pipeline teardown in video_stop_streaming() and log
errors instead of returning early on the first subdev stop failure.

---
 drivers/media/platform/qcom/camss/camss-video.c | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)
---
diff --git a/drivers/media/platform/qcom/camss/camss-video.c b/drivers/media/platform/qcom/camss/camss-video.c
index 831486e14754..baf83757e413 100644
--- a/drivers/media/platform/qcom/camss/camss-video.c
+++ b/drivers/media/platform/qcom/camss/camss-video.c
@@ -311,11 +311,9 @@ static void video_stop_streaming(struct vb2_queue *q)
 		subdev = media_entity_to_v4l2_subdev(entity);
 
 		ret = v4l2_subdev_call(subdev, video, s_stream, 0);
-
-		if (ret) {
-			dev_err(video->camss->dev, "Video pipeline stop failed: %d\n", ret);
-			return;
-		}
+		if (ret && ret != -ENOIOCTLCMD)
+			dev_err(video->camss->dev,
+				"Video pipeline stop failed: %d\n", ret);
 	}
 
 	video_device_pipeline_stop(vdev);
