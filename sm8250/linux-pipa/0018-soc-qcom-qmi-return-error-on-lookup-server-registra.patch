diff --git a/drivers/soc/qcom/qmi_interface.c b/drivers/soc/qcom/qmi_interface.c
index 16e2e24b6..99209a15d 100644
--- a/drivers/soc/qcom/qmi_interface.c
+++ b/drivers/soc/qcom/qmi_interface.c
@@ -162,7 +162,7 @@ static void qmi_recv_ctrl_pkt(struct qmi_handle *qmi,
 	}
 }
 
-static void qmi_send_new_lookup(struct qmi_handle *qmi, struct qmi_service *svc)
+static int qmi_send_new_lookup(struct qmi_handle *qmi, struct qmi_service *svc)
 {
 	struct qrtr_ctrl_pkt pkt;
 	struct sockaddr_qrtr sq;
@@ -185,10 +185,15 @@ static void qmi_send_new_lookup(struct qmi_handle *qmi, struct qmi_service *svc)
 	mutex_lock(&qmi->sock_lock);
 	if (qmi->sock) {
 		ret = kernel_sendmsg(qmi->sock, &msg, &iv, 1, sizeof(pkt));
-		if (ret < 0)
+		if (ret < 0) {
 			pr_err("failed to send lookup registration: %d\n", ret);
+			mutex_unlock(&qmi->sock_lock);
+			return ret;
+		}
 	}
 	mutex_unlock(&qmi->sock_lock);
+
+	return 0;
 }
 
 /**
@@ -219,13 +224,19 @@ int qmi_add_lookup(struct qmi_handle *qmi, unsigned int service,
 
 	list_add(&svc->list_node, &qmi->lookups);
 
-	qmi_send_new_lookup(qmi, svc);
+	/*
+	 * qmi_send_new_lookup may fail if QRTR is not ready yet
+	 * (e.g. during early boot). Log and keep the entry anyway;
+	 * it will be re-registered when qmi_handle_net_reset fires
+	 * after QRTR comes up.
+	 */
+	(void)qmi_send_new_lookup(qmi, svc);
 
 	return 0;
 }
 EXPORT_SYMBOL_GPL(qmi_add_lookup);
 
-static void qmi_send_new_server(struct qmi_handle *qmi, struct qmi_service *svc)
+static int qmi_send_new_server(struct qmi_handle *qmi, struct qmi_service *svc)
 {
 	struct qrtr_ctrl_pkt pkt;
 	struct sockaddr_qrtr sq;
@@ -250,10 +261,15 @@ static void qmi_send_new_server(struct qmi_handle *qmi, struct qmi_service *svc)
 	mutex_lock(&qmi->sock_lock);
 	if (qmi->sock) {
 		ret = kernel_sendmsg(qmi->sock, &msg, &iv, 1, sizeof(pkt));
-		if (ret < 0)
+		if (ret < 0) {
 			pr_err("send service registration failed: %d\n", ret);
+			mutex_unlock(&qmi->sock_lock);
+			return ret;
+		}
 	}
 	mutex_unlock(&qmi->sock_lock);
+
+	return 0;
 }
 
 /**
@@ -283,7 +299,13 @@ int qmi_add_server(struct qmi_handle *qmi, unsigned int service,
 
 	list_add(&svc->list_node, &qmi->services);
 
-	qmi_send_new_server(qmi, svc);
+	/*
+	 * qmi_send_new_server may fail if QRTR is not ready yet
+	 * (e.g. during early boot). Log and keep the entry anyway;
+	 * it will be re-registered when qmi_handle_net_reset fires
+	 * after QRTR comes up.
+	 */
+	(void)qmi_send_new_server(qmi, svc);
 
 	return 0;
 }
@@ -463,10 +485,10 @@ static void qmi_handle_net_reset(struct qmi_handle *qmi)
 	mutex_unlock(&qmi->sock_lock);
 
 	list_for_each_entry(svc, &qmi->lookups, list_node)
-		qmi_send_new_lookup(qmi, svc);
+		(void)qmi_send_new_lookup(qmi, svc);
 
 	list_for_each_entry(svc, &qmi->services, list_node)
-		qmi_send_new_server(qmi, svc);
+		(void)qmi_send_new_server(qmi, svc);
 }
 
 static void qmi_handle_message(struct qmi_handle *qmi,
