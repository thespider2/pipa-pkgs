# linux-pipa (Ubuntu)

Debian packaging for the Xiaomi Pad 6 kernel.

Source: kernel.org **7.1.4** + device patches (+ local single-DTB unify), staged
from `sm8250/linux-pipa/` by `scripts/build-ubuntu-debs.sh` into `debian/extras/`.
